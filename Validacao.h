#ifndef VALIDACAO_H
#define VALIDACAO_H

#include "structsModelo.h"
#include "Cliente.h"

int validaCodigoAtividade(int);
int validaInt(int);
int validaStatus(char);
int validaPrioridade(char* );
int validaDia(int );
int validaMes(int );
int validaAno(int );
int validaEstado(char* );
int validaTelefone(char* );

#endif
